Implement a function that opens the CSV file with fs::read_to_string, splits each line by the pipe delimiter, parses the header to map columns, then iterates over rows to create a Vec of Task structs.
