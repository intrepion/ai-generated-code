Implement CLI argument parsing by collecting std::env::args() into a Vec<String> and matching the first argument against known commands (list, add, edit, toggle, delete) to route to the appropriate handler.
