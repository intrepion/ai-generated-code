Implement the add operation by parsing the user-provided title argument, generating the next sequential id from the existing tasks (or starting at 1 if empty), creating a new Task with completed=false, and appending it to the CSV file.
