Implement the edit operation by parsing the task index from the user input, converting it to an integer, loading the tasks from CSV, updating the title of the task at that index, and rewriting the CSV file.
