Implement the toggle operation by parsing the task index from the user input, converting it to an integer, loading the tasks from CSV, flipping the completed boolean field of the task at that index, and rewriting the CSV file.
