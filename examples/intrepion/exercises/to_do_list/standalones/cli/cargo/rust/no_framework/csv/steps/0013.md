Implement the delete operation by parsing the task index from the user input, converting it to an integer, loading the tasks from CSV, removing the task at that index from the Vec, and rewriting the CSV file.
